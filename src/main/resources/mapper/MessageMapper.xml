<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chuanmei.bishe.dao.MessageDao">

    <insert id="addMessage">
        INSERT INTO message(account,cover_account,substance)
        VALUES(#{account,jsbcType = VARCHAR},#{coverAccount,jdbcType = VARCHAR},#{substance,jdbcType = substance})
    </insert>
    <delete id="deleteMessage">
        DELETE FROM message
        WHERE id = #{id,jdbcType = INTEGER}
    </delete>
    <select id="selectMessages" resultType="com.chuanmei.bishe.model.Message">
        SELECT m.id,m.start_time,m.substance,m.account,u.`name`
        FROM message m,user u
        WHERE m.account = u.account AND m.cover_account = #{coverAccount,jdbcType = VAECHAR}
    </select>
</mapper>